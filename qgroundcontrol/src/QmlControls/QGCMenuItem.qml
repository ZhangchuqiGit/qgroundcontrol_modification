// QtQuick.Control 1.x Menu
import QtQuick          2.6
import QtQuick.Controls 1.4
MenuItem {}
/*	MenuItem 供了一种熟悉的方式来响应被触发的菜单项。MenuItem 从 AbstractButton 继承其 API。
Button {
	 id: fileButton
	 text: "File"
	 onClicked: menu.open()
	 Menu {
		 id: menu
		 MenuItem {
			 text: "New..."
			 onTriggered: document.reset()
		 }
		 MenuItem {
			 text: "Open..."
			 onTriggered: openDialog.open()
		 }
		 MenuItem {
			 text: "Save"
			 onTriggered: saveDialog.open()
		 }
	 }
 }	*/
